version: '3.7'

services:
  web:
    build: .
    command:
      - /bin/sh
      - -c
      - |
        cd backend
        python manage.py migrate 
        python manage.py runserver 0.0.0.0:8080
    volumes:
      - ./:/usr/src/
    ports:
      - 8080:8080
